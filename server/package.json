{
  "name": "case-sim-leaderboard",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute case-sim-leaderboard --remote --file schema.sql",
    "db:init:local": "wrangler d1 execute case-sim-leaderboard --local --file schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
